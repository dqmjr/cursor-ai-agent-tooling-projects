{
  "mcpServers": {
    "sentinel": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-audit-gateway",
        "proxy",
        "--config",
        "REPLACE_WITH_ABS_PATH/sentinel.json"
      ]
    }
  }
}
